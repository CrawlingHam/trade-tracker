export const CREATE_ANONYMOUS_USERS = import.meta.env.CREATE_ANONYMOUS_USERS === "true";

export const DEFAULT_BOOTSTRAP_STEPS_STATE: Firebase.BootstrapStepsState = {
	settings: false,
	auth: false,
};
